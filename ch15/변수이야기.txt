변수의 선언자 var
ES6버전이후 let, const

var은 앞에 선언한 변수와 이름이 같을 경우 앞에 할당한것은 지워지고 새로 할당합니다.

중복된 이름을 사용해도 오류로 처리되지 않아서 버그를 발견하기 어려울 때가 있다.
=> 이것을 보완하기 위해 나온 변수를 할당하는 예약어가 let 이다.

var : 전역변수사용
let : 블록영역, 같은이름으로 변수를 선언하면 오류를 알려줍니다.
const : 블록영역, 상수
한번선언하고 값을 넣으면
값의 변경이 불가능하다.